import { useState } from "react";
import "./App.css";

function App() {

  const student = {
  };


  return (
    <div>
      <h2>Student List </h2>
    {stusent.map((student,index) =>
        <welcome key = {index} name ={name}/>)}
        <h3>welcome ,{name}</h3>
    </div>
    
  );
}

export default App;